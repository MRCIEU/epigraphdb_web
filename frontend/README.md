# `epigraphdb_web` frontend

Only the native environment setup is discussed here.
For setup via docker and the use of environment variables
check the master [README](../README.md).

## Setup

Prerequisites

- npm

Rough steps:

```
npm install
npm run serve
```
